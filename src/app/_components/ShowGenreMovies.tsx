import { useFetchDatainClient } from "@/hooks/useFetchDatainClient";

export const ShowGenreMovies = () => {
  //   const { data, isLoading } = useFetchDatainClient(
  // `/discover/movie?language=en&with_genres=${genreIds}&page=${genres}`
  //   );

  //   console.log("yum bna uu:", data);

  return <div>ShowGenreMovies</div>;
};
